# Frontend configuration (Vite)
# Copy to `.env` for local development.

# Backend API base URL (must include `/api`)
VITE_API_BASE=http://localhost:8000/api

# Optional: link shown in the "Source" button
VITE_SOURCE_URL=

